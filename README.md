Webber - Handy base image for webstesting
=========================================

This is a base image based on Ubuntu, with installed Python, Pip
Virtualenv and PhantomJS.

It's handy to test webstaff which need a PhantomJS driver for Selenium.
